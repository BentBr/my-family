name: ci

on:
    pull_request:
    push:
        branches: [main]
    # Dispatched by release-please.yml onto the release PR branch: the PR is
    # pushed via GITHUB_TOKEN, which can't trigger `pull_request`, but API
    # `workflow_dispatch` is the documented exception to that rule. Check runs
    # then attach to the PR head SHA natively (incl. the required `ci-gate`).
    workflow_dispatch:

env:
    CARGO_TERM_COLOR: always
    SQLX_OFFLINE: "true"
    RUSTFLAGS: "-D warnings"

# `contents: write` is required for the backend-coverage job to push the
# coverage badge JSON to the gh-pages branch. All other jobs are
# read-only; granting at workflow level is simpler than per-job and the
# push is gated by `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`.
permissions:
    contents: write

jobs:
    # ============================================================================
    # Detect which parts of the codebase changed. On push to main we run
    # everything (paths-filter can't compare to a base ref there).
    # ============================================================================
    changes:
        runs-on: ubuntu-latest
        outputs:
            backend: ${{ steps.filter.outputs.backend || steps.default.outputs.backend }}
            frontend: ${{ steps.filter.outputs.frontend || steps.default.outputs.frontend }}
        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0
            - if: ${{ github.event_name == 'pull_request' }}
              uses: dorny/paths-filter@v4
              id: filter
              with:
                  filters: |
                      # Any change to CI/infra files flips BOTH groups so the
                      # full pipeline runs (workflow/docker/compose/scripts
                      # changes must exercise everything — otherwise a broken
                      # CI yaml could merge with all jobs skipped).
                      infra: &infra
                          - '.github/workflows/**'
                          - '.docker/**'
                          - '.dockerignore'
                          - 'compose.yaml'
                          - 'scripts/**'
                          - 'release-please-config.json'
                          - '.release-please-manifest.json'
                      backend:
                          - *infra
                          - 'crates/**'
                          - 'migrations/**'
                          - 'Cargo.toml'
                          - 'Cargo.lock'
                          - '.sqlx/**'
                          - 'deny.toml'
                          - 'rust-toolchain.toml'
                      frontend:
                          - *infra
                          - 'fe/**'
            - if: ${{ github.event_name != 'pull_request' }}
              id: default
              run: |
                  echo "backend=true" >> "$GITHUB_OUTPUT"
                  echo "frontend=true" >> "$GITHUB_OUTPUT"

    # ============================================================================
    # Commit Lint - Validates Conventional Commits on every PR
    # ============================================================================
    commit-lint:
        if: ${{ github.event_name == 'pull_request' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
              with:
                  fetch-depth: 0
            - name: Validate Conventional Commits
              env:
                  BASE_SHA: ${{ github.event.pull_request.base.sha }}
                  HEAD_SHA: ${{ github.event.pull_request.head.sha }}
              run: |
                  conventional_pattern='^(feat|fix|docs|style|refactor|perf|test|build|ci|chore|revert)(\(.+\))?(!)?: .+'
                  merge_pattern='^Merge '
                  invalid_commits=()
                  while IFS= read -r commit_sha; do
                      [[ -z "$commit_sha" ]] && continue
                      subject="$(git log -1 --format='%s' "$commit_sha")"
                      if [[ "$subject" =~ $merge_pattern ]]; then
                          continue
                      fi
                      if ! [[ "$subject" =~ $conventional_pattern ]]; then
                          invalid_commits+=("$commit_sha: $subject")
                      fi
                  done < <(git rev-list "$BASE_SHA..$HEAD_SHA")

                  if [[ ${#invalid_commits[@]} -gt 0 ]]; then
                      echo "::error::The following commits do not follow Conventional Commits format:"
                      for msg in "${invalid_commits[@]}"; do
                          echo "  - $msg"
                      done
                      echo ""
                      echo "Expected format: <type>[optional scope][!]: <description>"
                      echo "Types: feat, fix, docs, style, refactor, perf, test, build, ci, chore, revert"
                      exit 1
                  fi
                  echo "All commits follow Conventional Commits format"

    # ============================================================================
    # Branch Name Validation - Runs on every PR
    # ============================================================================
    branch-name:
        if: ${{ github.event_name == 'pull_request' }}
        runs-on: ubuntu-latest
        steps:
            - name: Validate branch name
              env:
                  BRANCH_NAME: ${{ github.head_ref }}
              run: |
                  regex='^(main|master|develop|(release-please|dependabot|renovate)(--|/).+|(feat|fix|hotfix|chore|docs|refactor|perf|test|build|ci|style|revert|release)/[a-z0-9][a-z0-9._-]*)$'
                  if [[ ! "$BRANCH_NAME" =~ $regex ]]; then
                      echo "::error::Branch name '$BRANCH_NAME' does not follow the naming convention."
                      echo ""
                      echo "Expected: <type>/<description>"
                      echo "Types: feat, fix, hotfix, chore, docs, refactor, perf, test, build, ci, style, revert, release"
                      echo "Description: lowercase alphanumeric + . _ - (must start alphanumeric; no uppercase)"
                      echo ""
                      echo "Exempt branches: main, master, develop, release-please--*, dependabot/*, renovate/*"
                      exit 1
                  fi
                  echo "Branch name '$BRANCH_NAME' is valid"

    # ============================================================================
    # Backend prepare - warm cargo caches + cargo-bin tools once. Downstream
    # backend jobs reuse the cargo build cache via swatinem/rust-cache@v2.
    # ============================================================================
    backend-prepare:
        needs: changes
        if: ${{ needs.changes.outputs.backend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: dtolnay/rust-toolchain@nightly
              with:
                  components: rustfmt, clippy
            - uses: Swatinem/rust-cache@v2
            - name: Cache cargo bin
              uses: actions/cache@v5
              with:
                  path: ~/.cargo/bin
                  key: ${{ runner.os }}-cargo-bin-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml') }}
                  restore-keys: |
                      ${{ runner.os }}-cargo-bin-
            - name: Install cargo-deny
              run: |
                  if ! command -v cargo-deny >/dev/null 2>&1; then
                      cargo install cargo-deny --locked
                  fi
            - name: Install sqlx-cli
              run: |
                  if ! command -v sqlx >/dev/null 2>&1; then
                      cargo install sqlx-cli --no-default-features --features postgres --locked
                  fi
            - name: Install cargo-llvm-cov
              run: |
                  if ! command -v cargo-llvm-cov >/dev/null 2>&1; then
                      cargo install cargo-llvm-cov --locked
                  fi

    backend-clippy:
        needs: [changes, backend-prepare]
        if: ${{ needs.changes.outputs.backend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: dtolnay/rust-toolchain@nightly
              with:
                  components: clippy
            - uses: Swatinem/rust-cache@v2
            - run: cargo +nightly clippy --workspace --all-targets --all-features -- -D clippy::all -D warnings -D clippy::pedantic -D clippy::nursery

    backend-format:
        needs: [changes, backend-prepare]
        if: ${{ needs.changes.outputs.backend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: dtolnay/rust-toolchain@nightly
              with:
                  components: rustfmt
            - run: cargo fmt --all -- --check

    backend-audit:
        needs: [changes, backend-prepare]
        if: ${{ needs.changes.outputs.backend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: dtolnay/rust-toolchain@nightly
            - uses: Swatinem/rust-cache@v2
            - name: Restore cargo bin
              uses: actions/cache@v5
              with:
                  path: ~/.cargo/bin
                  key: ${{ runner.os }}-cargo-bin-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml') }}
                  restore-keys: |
                      ${{ runner.os }}-cargo-bin-
            - name: Install cargo-deny
              run: |
                  if ! command -v cargo-deny >/dev/null 2>&1; then
                      cargo install cargo-deny --locked
                  fi
            - run: cargo deny check

    backend-tests:
        needs: [changes, backend-prepare]
        if: ${{ needs.changes.outputs.backend == 'true' }}
        runs-on: ubuntu-latest
        services:
            postgres:
                image: postgres:18-alpine
                env:
                    POSTGRES_USER: my_fam_tree
                    POSTGRES_PASSWORD: my_fam_tree
                    POSTGRES_DB: my_fam_tree
                ports: ["3458:5432"]
                options: >-
                    --health-cmd "pg_isready -U my_fam_tree"
                    --health-interval 5s --health-timeout 3s --health-retries 10
            redis:
                image: redis:7-alpine
                ports: ["6379:6379"]
                options: >-
                    --health-cmd "redis-cli ping"
                    --health-interval 5s --health-timeout 3s --health-retries 10
            mailpit:
                image: axllent/mailpit:latest
                ports: ["1025:1025", "8025:8025"]
        env:
            RUN_ID: ${{ github.run_id }}
            APP_ENV: development
            LOG_FORMAT: pretty
            RUST_LOG: info
            DATABASE_URL: postgres://my_fam_tree:my_fam_tree@localhost:3458/my_fam_tree
            DATABASE_MAX_CONNECTIONS: "10"
            DATABASE_ACQUIRE_TIMEOUT_SECONDS: "5"
            DATABASE_STATEMENT_TIMEOUT_MS: "30000"
            REDIS_URL: redis://localhost:6379/0
            REDIS_MAX_CONNECTIONS: "10"
            REDIS_KEY_PREFIX: "my-fam-tree:"
            EMAIL_DSN: smtp://localhost:1025
            EMAIL_FROM_NAME: my-fam-tree
            EMAIL_FROM_ADDRESS: no-reply@my-fam-tree.local
            EMAIL_TIMEOUT_SECONDS: "10"
            API_HOST: 127.0.0.1
            API_PORT: "8080"
            API_PUBLIC_URL: http://localhost:8080
            WEB_PUBLIC_URL: http://localhost:5173
            CORS_ALLOWED_ORIGINS: http://localhost:5173
            API_ENABLE_DOCS: "false"
            API_METRICS_BIND: 127.0.0.1:9090
            COOKIE_DOMAIN: ""
            COOKIE_SECURE: "false"
            COOKIE_SAMESITE_ACCESS: Lax
            COOKIE_SAMESITE_REFRESH: Strict
            JWT_ISSUER: my-fam-tree
            JWT_AUDIENCE: my-fam-tree-app
            JWT_ACCESS_TTL_SECONDS: "900"
            JWT_REFRESH_TTL_SECONDS: "2592000"
            JWT_REFRESH_ABSOLUTE_TTL_SECONDS: "7776000"
            MAGIC_LINK_TTL_SECONDS: "900"
            INVITE_TTL_SECONDS: "1209600"
            # The e2e suite sends 100+ magic-link requests from one runner
            # IP. 200 was not enough; pick a number that's safely above any
            # plausible suite count.
            MAGIC_LINK_RATE_PER_EMAIL_PER_HOUR: "1000"
            MAGIC_LINK_RATE_PER_IP_PER_HOUR: "10000"
            # Storage: CI has no MinIO/S3 sidecar, so the local driver
            # writes anything to a temp dir on the runner. The fields below
            # are required by the config schema even when unused.
            STORAGE_DRIVER: local
            STORAGE_BUCKET: ci-uploads
            STORAGE_REGION: us-east-1
            STORAGE_ACCESS_KEY_ID: ""
            STORAGE_SECRET_ACCESS_KEY: ""
            STORAGE_FORCE_PATH_STYLE: "false"
        steps:
            - uses: actions/checkout@v4
            - uses: dtolnay/rust-toolchain@nightly
            - uses: Swatinem/rust-cache@v2
            # The seed binary's `Config::load_from_env` validates JWT_*; the
            # tests themselves don't read these values (testcontainers tests
            # spin up isolated postgres containers per test — see the seed
            # step comment below for why this step is run anyway), but a
            # missing JWT_* would fail config load before run_seed gets a
            # chance to insert anything. Same gen-jwt-keys → .env pattern
            # used by frontend-e2e.
            - name: Generate Ed25519 JWT keypair into .env
              run: |
                  cargo run -p my-fam-tree-api --bin gen-jwt-keys -- "ci-${RUN_ID}" > /tmp/jwt.env
                  grep -E '^JWT_' /tmp/jwt.env >> .env
            - name: Apply database migrations
              run: cargo run -p my-fam-tree-migrator --bin run_migrations
            - name: Seed dev data
              # The backend-tests integration tests spin up isolated postgres
              # containers via testcontainers (see crates/api/tests/common/mod.rs),
              # so they do NOT read from this service-container DB. This step
              # is a documentation/spec-compliance no-op safety net per the
              # Phase 2c plan, NOT a functional dependency of `cargo test`.
              run: cargo run -p my-fam-tree-seeder
            - run: cargo test --workspace --all-targets

    backend-build:
        needs: [changes, backend-prepare]
        if: ${{ needs.changes.outputs.backend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: dtolnay/rust-toolchain@nightly
            - uses: Swatinem/rust-cache@v2
            - run: cargo build --workspace --all-targets

    backend-coverage:
        needs: [changes, backend-prepare]
        if: ${{ needs.changes.outputs.backend == 'true' }}
        runs-on: ubuntu-latest
        services:
            postgres:
                image: postgres:18-alpine
                env:
                    POSTGRES_USER: my_fam_tree
                    POSTGRES_PASSWORD: my_fam_tree
                    POSTGRES_DB: my_fam_tree
                ports: ["3458:5432"]
                options: >-
                    --health-cmd "pg_isready -U my_fam_tree"
                    --health-interval 5s --health-timeout 3s --health-retries 10
            redis:
                image: redis:7-alpine
                ports: ["6379:6379"]
                options: >-
                    --health-cmd "redis-cli ping"
                    --health-interval 5s --health-timeout 3s --health-retries 10
            mailpit:
                image: axllent/mailpit:latest
                ports: ["1025:1025", "8025:8025"]
        env:
            RUN_ID: ${{ github.run_id }}
            APP_ENV: development
            LOG_FORMAT: pretty
            RUST_LOG: info
            DATABASE_URL: postgres://my_fam_tree:my_fam_tree@localhost:3458/my_fam_tree
            DATABASE_MAX_CONNECTIONS: "10"
            DATABASE_ACQUIRE_TIMEOUT_SECONDS: "5"
            DATABASE_STATEMENT_TIMEOUT_MS: "30000"
            REDIS_URL: redis://localhost:6379/0
            REDIS_MAX_CONNECTIONS: "10"
            REDIS_KEY_PREFIX: "my-fam-tree:"
            EMAIL_DSN: smtp://localhost:1025
            EMAIL_FROM_NAME: my-fam-tree
            EMAIL_FROM_ADDRESS: no-reply@my-fam-tree.local
            EMAIL_TIMEOUT_SECONDS: "10"
            API_HOST: 127.0.0.1
            API_PORT: "8080"
            API_PUBLIC_URL: http://localhost:8080
            WEB_PUBLIC_URL: http://localhost:5173
            CORS_ALLOWED_ORIGINS: http://localhost:5173
            API_ENABLE_DOCS: "false"
            API_METRICS_BIND: 127.0.0.1:9090
            COOKIE_DOMAIN: ""
            COOKIE_SECURE: "false"
            COOKIE_SAMESITE_ACCESS: Lax
            COOKIE_SAMESITE_REFRESH: Strict
            JWT_ISSUER: my-fam-tree
            JWT_AUDIENCE: my-fam-tree-app
            JWT_ACCESS_TTL_SECONDS: "900"
            JWT_REFRESH_TTL_SECONDS: "2592000"
            JWT_REFRESH_ABSOLUTE_TTL_SECONDS: "7776000"
            MAGIC_LINK_TTL_SECONDS: "900"
            INVITE_TTL_SECONDS: "1209600"
            # The e2e suite sends 100+ magic-link requests from one runner
            # IP. 200 was not enough; pick a number that's safely above any
            # plausible suite count.
            MAGIC_LINK_RATE_PER_EMAIL_PER_HOUR: "1000"
            MAGIC_LINK_RATE_PER_IP_PER_HOUR: "10000"
            # Storage: CI has no MinIO/S3 sidecar, so the local driver
            # writes anything to a temp dir on the runner. The fields below
            # are required by the config schema even when unused.
            STORAGE_DRIVER: local
            STORAGE_BUCKET: ci-uploads
            STORAGE_REGION: us-east-1
            STORAGE_ACCESS_KEY_ID: ""
            STORAGE_SECRET_ACCESS_KEY: ""
            STORAGE_FORCE_PATH_STYLE: "false"
        steps:
            - uses: actions/checkout@v4
            - uses: dtolnay/rust-toolchain@nightly
              with:
                  components: llvm-tools-preview
            - uses: Swatinem/rust-cache@v2
            - name: Restore cargo bin
              uses: actions/cache@v5
              with:
                  path: ~/.cargo/bin
                  key: ${{ runner.os }}-cargo-bin-${{ hashFiles('Cargo.lock', 'rust-toolchain.toml') }}
                  restore-keys: |
                      ${{ runner.os }}-cargo-bin-
            - name: Install cargo-llvm-cov
              run: |
                  if ! command -v cargo-llvm-cov >/dev/null 2>&1; then
                      cargo install cargo-llvm-cov --locked
                  fi
            # Like backend-tests: tests use isolated testcontainers instances,
            # so this migrate+seed pair against the service-container DB is a
            # no-op safety net for spec compliance, not a functional gate.
            - name: Generate Ed25519 JWT keypair into .env
              run: |
                  cargo run -p my-fam-tree-api --bin gen-jwt-keys -- "ci-${RUN_ID}" > /tmp/jwt.env
                  grep -E '^JWT_' /tmp/jwt.env >> .env
            - name: Apply database migrations
              run: cargo run -p my-fam-tree-migrator --bin run_migrations
            - name: Seed dev data
              run: cargo run -p my-fam-tree-seeder
            # Exclusion regex + threshold live in scripts/coverage.sh — single
            # source of truth for both CI and local `rdt coverage`.
            - name: Run coverage (profdata + LCOV + JSON)
              run: |
                  ./scripts/coverage.sh profile
                  ./scripts/coverage.sh lcov
                  ./scripts/coverage.sh json
            - name: Append coverage summary to GitHub step summary
              run: |
                  {
                      echo "## Rust coverage"
                      echo ""
                      echo '```'
                      ./scripts/coverage.sh summary
                      echo '```'
                  } >> "$GITHUB_STEP_SUMMARY"
            - name: Enforce coverage threshold
              run: ./scripts/coverage.sh check
            - uses: actions/upload-artifact@v7
              with:
                  name: rust-coverage
                  path: |
                      coverage/rust.lcov
                      coverage/rust.json
                  retention-days: 14
            - name: Publish coverage badge to gh-pages
              # Only on push to main: avoids gh-pages churn during PR runs and
              # avoids cross-fork token issues. Uses GITHUB_TOKEN with the
              # default contents:write granted at the workflow level (no PAT).
              if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  REPO: ${{ github.repository }}
              run: |
                  PCT=$(jq -r '.data[0].totals.lines.percent | tostring | .[0:5]' coverage/rust.json)
                  COLOR=$(awk -v p="$PCT" 'BEGIN{
                      if (p+0 >= 90) print "brightgreen";
                      else if (p+0 >= 80) print "green";
                      else if (p+0 >= 70) print "yellowgreen";
                      else if (p+0 >= 60) print "yellow";
                      else if (p+0 >= 50) print "orange";
                      else print "red";
                  }')
                  printf '{"schemaVersion":1,"label":"BE coverage","message":"%s%%","color":"%s"}\n' "$PCT" "$COLOR" > /tmp/coverage.json

                  # Clone gh-pages (or initialize it if it doesn't exist yet).
                  cd /tmp
                  rm -rf pages
                  if git clone --branch gh-pages --depth 1 "https://x-access-token:${GH_TOKEN}@github.com/${REPO}.git" pages 2>/dev/null; then
                      cd pages
                  else
                      mkdir pages && cd pages
                      git init -b gh-pages
                      git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${REPO}.git"
                  fi
                  git config user.name 'github-actions[bot]'
                  git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
                  cp /tmp/coverage.json coverage.json
                  git add coverage.json
                  if git diff --staged --quiet; then
                      echo "coverage unchanged — skipping push"
                  else
                      git commit -m "chore(coverage): update badge to ${PCT}%"
                      git push origin gh-pages
                  fi

    backend-ts-bindings:
        name: TS bindings up to date
        needs: [changes, backend-prepare]
        if: ${{ needs.changes.outputs.backend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: dtolnay/rust-toolchain@nightly
            - uses: Swatinem/rust-cache@v2
            - name: Dump live OpenAPI
              run: cargo run -p my-fam-tree-openapi --bin openapi-dump > /tmp/openapi.json
            - name: Diff against committed fe/openapi.json
              run: diff -u fe/openapi.json /tmp/openapi.json

    # ============================================================================
    # Frontend prepare - install deps once into the repo-local pnpm store.
    # Downstream FE jobs do `pnpm install --frozen-lockfile` again; pnpm reuses
    # the cached store via fe/.npmrc (`store-dir=.pnpm-store`).
    # ============================================================================
    frontend-prepare:
        needs: changes
        if: ${{ needs.changes.outputs.frontend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v6
              with:
                  version: 10.33.4
            - uses: actions/setup-node@v6
              with:
                  node-version: 22
            # setup-node's built-in pnpm cache assumes a global store at
            # ~/.local/share/pnpm/store; ours lives at fe/.pnpm-store
            # (fe/.npmrc). Cache the repo-local store directly.
            - uses: actions/cache@v5
              with:
                  path: fe/.pnpm-store
                  key: ${{ runner.os }}-pnpm-store-${{ hashFiles('fe/pnpm-lock.yaml') }}
                  restore-keys: ${{ runner.os }}-pnpm-store-
            - run: cd fe && pnpm install --frozen-lockfile

    frontend-lint:
        needs: [changes, frontend-prepare]
        if: ${{ needs.changes.outputs.frontend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v6
              with:
                  version: 10.33.4
            - uses: actions/setup-node@v6
              with:
                  node-version: 22
            - uses: actions/cache@v5
              with:
                  path: fe/.pnpm-store
                  key: ${{ runner.os }}-pnpm-store-${{ hashFiles('fe/pnpm-lock.yaml') }}
                  restore-keys: ${{ runner.os }}-pnpm-store-
            - run: cd fe && pnpm install --frozen-lockfile
            - run: cd fe && pnpm openapi-codegen
            - run: cd fe && pnpm lint
            - run: cd fe && pnpm knip

    frontend-typecheck:
        needs: [changes, frontend-prepare]
        if: ${{ needs.changes.outputs.frontend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v6
              with:
                  version: 10.33.4
            - uses: actions/setup-node@v6
              with:
                  node-version: 22
            - uses: actions/cache@v5
              with:
                  path: fe/.pnpm-store
                  key: ${{ runner.os }}-pnpm-store-${{ hashFiles('fe/pnpm-lock.yaml') }}
                  restore-keys: ${{ runner.os }}-pnpm-store-
            - run: cd fe && pnpm install --frozen-lockfile
            - run: cd fe && pnpm openapi-codegen
            - run: cd fe && pnpm typecheck

    frontend-tests:
        needs: [changes, frontend-prepare]
        if: ${{ needs.changes.outputs.frontend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v6
              with:
                  version: 10.33.4
            - uses: actions/setup-node@v6
              with:
                  node-version: 22
            - uses: actions/cache@v5
              with:
                  path: fe/.pnpm-store
                  key: ${{ runner.os }}-pnpm-store-${{ hashFiles('fe/pnpm-lock.yaml') }}
                  restore-keys: ${{ runner.os }}-pnpm-store-
            - run: cd fe && pnpm install --frozen-lockfile
            - run: cd fe && pnpm openapi-codegen
            # Vitest only - Playwright runs in the frontend-e2e job.
            - run: cd fe && pnpm test

    frontend-build:
        needs: [changes, frontend-prepare]
        if: ${{ needs.changes.outputs.frontend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v6
              with:
                  version: 10.33.4
            - uses: actions/setup-node@v6
              with:
                  node-version: 22
            - uses: actions/cache@v5
              with:
                  path: fe/.pnpm-store
                  key: ${{ runner.os }}-pnpm-store-${{ hashFiles('fe/pnpm-lock.yaml') }}
                  restore-keys: ${{ runner.os }}-pnpm-store-
            - run: cd fe && pnpm install --frozen-lockfile
            - run: cd fe && pnpm openapi-codegen
            - run: cd fe && pnpm build

    frontend-coverage:
        needs: [changes, frontend-prepare]
        if: ${{ needs.changes.outputs.frontend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: pnpm/action-setup@v6
              with:
                  version: 10.33.4
            - uses: actions/setup-node@v6
              with:
                  node-version: 22
            - uses: actions/cache@v5
              with:
                  path: fe/.pnpm-store
                  key: ${{ runner.os }}-pnpm-store-${{ hashFiles('fe/pnpm-lock.yaml') }}
                  restore-keys: ${{ runner.os }}-pnpm-store-
            - run: cd fe && pnpm install --frozen-lockfile
            - run: cd fe && pnpm openapi-codegen
            - name: Run FE coverage
              run: cd fe && pnpm coverage
            - name: Append FE coverage summary to step summary
              run: |
                  {
                      echo "## Frontend coverage"
                      echo ""
                      echo '```'
                      jq -r '"lines:      \(.total.lines.pct)%\nstatements: \(.total.statements.pct)%\nfunctions:  \(.total.functions.pct)%\nbranches:   \(.total.branches.pct)%"' fe/coverage/coverage-summary.json
                      echo '```'
                  } >> "$GITHUB_STEP_SUMMARY"
            - uses: actions/upload-artifact@v7
              with:
                  name: fe-coverage
                  path: |
                      fe/coverage/lcov.info
                      fe/coverage/coverage-summary.json
                  retention-days: 14
            - name: Publish FE coverage badge to gh-pages
              # Mirror of the backend-coverage gh-pages push. Only on push to
              # main — avoids gh-pages churn during PRs and cross-fork token
              # issues. Uses GITHUB_TOKEN with workflow-level contents:write.
              if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/main' }}
              env:
                  GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
                  REPO: ${{ github.repository }}
              run: |
                  PCT=$(jq -r '.total.lines.pct | tostring | .[0:5]' fe/coverage/coverage-summary.json)
                  COLOR=$(awk -v p="$PCT" 'BEGIN{
                      if (p+0 >= 90) print "brightgreen";
                      else if (p+0 >= 80) print "green";
                      else if (p+0 >= 70) print "yellowgreen";
                      else if (p+0 >= 60) print "yellow";
                      else if (p+0 >= 50) print "orange";
                      else print "red";
                  }')
                  printf '{"schemaVersion":1,"label":"FE coverage","message":"%s%%","color":"%s"}\n' "$PCT" "$COLOR" > /tmp/coverage-fe.json

                  cd /tmp
                  rm -rf pages
                  if git clone --branch gh-pages --depth 1 "https://x-access-token:${GH_TOKEN}@github.com/${REPO}.git" pages 2>/dev/null; then
                      cd pages
                  else
                      mkdir pages && cd pages
                      git init -b gh-pages
                      git remote add origin "https://x-access-token:${GH_TOKEN}@github.com/${REPO}.git"
                  fi
                  git config user.name 'github-actions[bot]'
                  git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
                  cp /tmp/coverage-fe.json coverage-fe.json
                  git add coverage-fe.json
                  if git diff --staged --quiet; then
                      echo "FE coverage unchanged — skipping push"
                  else
                      git commit -m "chore(coverage): update FE badge to ${PCT}%"
                      git push origin gh-pages
                  fi

    # ============================================================================
    # Frontend E2E - full stack: cargo build api + pnpm dev + Playwright.
    # Triggered when either backend or frontend changed (the wire crosses both).
    # ============================================================================
    frontend-e2e:
        needs: changes
        if: ${{ needs.changes.outputs.frontend == 'true' || needs.changes.outputs.backend == 'true' }}
        runs-on: ubuntu-latest
        services:
            postgres:
                image: postgres:18-alpine
                env:
                    POSTGRES_USER: my_fam_tree
                    POSTGRES_PASSWORD: my_fam_tree
                    POSTGRES_DB: my_fam_tree
                ports: ["3458:5432"]
                options: >-
                    --health-cmd "pg_isready -U my_fam_tree"
                    --health-interval 5s --health-timeout 3s --health-retries 10
            redis:
                image: redis:7-alpine
                ports: ["6379:6379"]
                options: >-
                    --health-cmd "redis-cli ping"
                    --health-interval 5s --health-timeout 3s --health-retries 10
            mailpit:
                image: axllent/mailpit:latest
                ports: ["1025:1025", "8025:8025"]
        env:
            RUN_ID: ${{ github.run_id }}
            APP_ENV: development
            LOG_FORMAT: pretty
            RUST_LOG: info,my_fam_tree=debug
            DATABASE_URL: postgres://my_fam_tree:my_fam_tree@localhost:3458/my_fam_tree
            DATABASE_MAX_CONNECTIONS: "20"
            DATABASE_ACQUIRE_TIMEOUT_SECONDS: "5"
            DATABASE_STATEMENT_TIMEOUT_MS: "30000"
            REDIS_URL: redis://localhost:6379/0
            REDIS_MAX_CONNECTIONS: "10"
            REDIS_KEY_PREFIX: "my-fam-tree:"
            EMAIL_DSN: smtp://localhost:1025
            EMAIL_FROM_NAME: my-fam-tree
            EMAIL_FROM_ADDRESS: no-reply@my-fam-tree.local
            EMAIL_TIMEOUT_SECONDS: "10"
            API_HOST: 127.0.0.1
            API_PORT: "8080"
            API_PUBLIC_URL: http://localhost:8080
            WEB_PUBLIC_URL: http://localhost:5173
            CORS_ALLOWED_ORIGINS: http://localhost:5173
            API_ENABLE_DOCS: "true"
            API_METRICS_BIND: 127.0.0.1:9090
            COOKIE_DOMAIN: ""
            COOKIE_SECURE: "false"
            COOKIE_SAMESITE_ACCESS: Lax
            COOKIE_SAMESITE_REFRESH: Strict
            JWT_ISSUER: my-fam-tree
            JWT_AUDIENCE: my-fam-tree-app
            JWT_ACCESS_TTL_SECONDS: "900"
            JWT_REFRESH_TTL_SECONDS: "2592000"
            JWT_REFRESH_ABSOLUTE_TTL_SECONDS: "7776000"
            MAGIC_LINK_TTL_SECONDS: "900"
            INVITE_TTL_SECONDS: "1209600"
            # The e2e suite sends 100+ magic-link requests from one runner
            # IP. 200 was not enough; pick a number that's safely above any
            # plausible suite count.
            MAGIC_LINK_RATE_PER_EMAIL_PER_HOUR: "1000"
            MAGIC_LINK_RATE_PER_IP_PER_HOUR: "10000"
            # Storage: CI has no MinIO/S3 sidecar, so the local driver
            # writes anything to a temp dir on the runner. The fields below
            # are required by the config schema even when unused.
            STORAGE_DRIVER: local
            STORAGE_BUCKET: ci-uploads
            STORAGE_REGION: us-east-1
            STORAGE_ACCESS_KEY_ID: ""
            STORAGE_SECRET_ACCESS_KEY: ""
            STORAGE_FORCE_PATH_STYLE: "false"
            VITE_API_BASE_URL: http://localhost:8080
        steps:
            - uses: actions/checkout@v4
            - uses: dtolnay/rust-toolchain@nightly
            - uses: Swatinem/rust-cache@v2
            - uses: pnpm/action-setup@v6
              with:
                  version: 10.33.4
            - uses: actions/setup-node@v6
              with:
                  node-version: 22
            - uses: actions/cache@v5
              with:
                  path: fe/.pnpm-store
                  key: ${{ runner.os }}-pnpm-store-${{ hashFiles('fe/pnpm-lock.yaml') }}
                  restore-keys: ${{ runner.os }}-pnpm-store-
            - name: Generate Ed25519 JWT keypair into .env
              # gen-jwt-keys emits JWT_PRIVATE_KEY='-----BEGIN…\n…\n-----END…'
              # — single-quoted with literal `\n` sequences. Writing that
              # through $GITHUB_ENV mangles the multi-line content (quotes
              # bleed in / NUL byte). The api binary reads `.env` via dotenvy
              # when APP_ENV=development, which handles single-quoted values
              # correctly. Write directly to `.env` so the api process picks
              # them up at startup; no shell env round-trip.
              run: |
                  cargo run -p my-fam-tree-api --bin gen-jwt-keys -- "ci-${RUN_ID}" > /tmp/jwt.env
                  grep -E '^JWT_' /tmp/jwt.env >> .env
            - name: Apply database migrations
              # Without this the api boots and immediately 500s on every
              # endpoint that touches a table (`relation "users" does not
              # exist`). Uses the same DATABASE_URL the api process will pick
              # up from the workflow env block.
              run: cargo run -p my-fam-tree-migrator --bin run_migrations
            - name: Seed dev data
              # Deterministic UPSERT seed (3 users, 1 family, 8 persons, etc).
              # The Playwright e2e suite can rely on these stable UUIDs
              # without per-test fixtures. Idempotent: a re-run is a no-op.
              run: cargo run -p my-fam-tree-seeder
            - name: Build api binary
              # Debug profile is fine for an e2e - release would add minutes
              # for no behavior difference.
              run: cargo build -p my-fam-tree-api --bin api
            - name: Start api in background
              run: |
                  ./target/debug/api > /tmp/api.log 2>&1 &
                  echo "API_PID=$!" >> "$GITHUB_ENV"
                  for i in $(seq 1 30); do
                      curl -fsS http://localhost:8080/api/v1/health >/dev/null && break
                      echo "waiting for api..."
                      sleep 2
                  done
            - name: Build worker (test-fixtures)
              # The test-fixtures feature adds the POST /__test/advance-clock
              # listener the reminders e2e drives. NEVER built into the
              # published image (see .docker/worker.Dockerfile).
              run: cargo build -p my-fam-tree-worker --bin worker --features test-fixtures
            - name: Start worker in background
              # The job env block already provides DATABASE_URL / REDIS_URL /
              # EMAIL_* / WEB_PUBLIC_URL; the WORKER_* knobs are worker-only so
              # they're set here. A short tick keeps the auto-loop cheap; the
              # e2e drives scheduling explicitly via advance-clock.
              env:
                  WORKER_TICK_INTERVAL_SECONDS: "5"
                  WORKER_LEADER_LEASE_SECONDS: "60"
                  WORKER_LEADER_REFRESH_SECONDS: "10"
                  WORKER_MAX_RETRIES: "5"
                  WORKER_RETRY_BACKOFF_MIN_SECONDS: "60"
                  WORKER_RETRY_BACKOFF_MAX_SECONDS: "43200"
                  WORKER_METRICS_BIND: 127.0.0.1:9091
                  # Janitor + outbox knobs — without these the worker config
                  # load fails at startup and the e2e suite times out because
                  # every test waits for an email that never lands.
                  WORKER_JANITOR_INTERVAL_SECONDS: "60"
                  WORKER_JANITOR_GRACE_SECONDS: "300"
                  WORKER_OUTBOX_POLL_SECONDS: "1"
                  WORKER_OUTBOX_POOL_SIZE: "4"
              run: |
                  ./target/debug/worker > /tmp/worker.log 2>&1 &
                  echo "WORKER_PID=$!" >> "$GITHUB_ENV"
                  # Readiness: the listener answers advance-clock once bound.
                  # Probe with the CURRENT time — advance-clock MUTATES the
                  # OffsetClock, so the probe target becomes the worker's
                  # "now" for the whole suite. The previous 2099 probe left
                  # the worker ~73 years in the future: the janitor's cutoff
                  # then classified EVERY freshly-minted single-use token
                  # (magic-link / invite / refresh) as expired and deleted
                  # them on each sweep, so sign-ins whose mint→consume window
                  # straddled a sweep intermittently 401'd ("link may have
                  # expired") — the long-standing e2e auth flake. `now` keeps
                  # the offset ≈ 0 (so api-inserted outbox rows are still due
                  # and drain — the failure mode of the even older 2020 probe,
                  # which set a NEGATIVE offset) without time-travelling the
                  # janitor. Fail the step explicitly if the listener never
                  # answers.
                  ready=0
                  for i in $(seq 1 30); do
                      if curl -fsS -X POST http://localhost:9091/__test/advance-clock \
                        -H 'content-type: application/json' \
                        -d "{\"to\":\"$(date -u +%Y-%m-%dT%H:%M:%SZ)\"}" >/dev/null; then
                          ready=1
                          break
                      fi
                      echo "waiting for worker test listener..."
                      sleep 1
                  done
                  if [ "$ready" != "1" ]; then
                      echo "worker never came up; dumping log:"
                      cat /tmp/worker.log || true
                      exit 1
                  fi
            - name: Install FE deps + regenerate openapi types
              working-directory: fe
              run: |
                  pnpm install --frozen-lockfile
                  pnpm openapi-codegen
            - name: Install Playwright Chromium
              working-directory: fe
              run: pnpm exec playwright install --with-deps chromium
            - name: Build FE for production
              # We deliberately E2E the production bundle (not `pnpm dev`)
              # because Vite dev's on-demand module loading + HMR
              # instrumentation can re-evaluate components mid-flight under
              # headless Chromium, causing legitimate single-use actions
              # (magic-link consume, form submit, menu-item click) to
              # double-fire. The production build is a single pre-bundled
              # SPA that matches what we actually ship.
              working-directory: fe
              run: pnpm build
            - name: Start Vite preview server in background
              working-directory: fe
              run: |
                  pnpm preview --port 5173 --strictPort > /tmp/vite.log 2>&1 &
                  echo "VITE_PID=$!" >> "$GITHUB_ENV"
                  for i in $(seq 1 30); do
                      curl -fsS http://localhost:5173/ >/dev/null && break
                      echo "waiting for vite preview..."
                      sleep 2
                  done
            - name: Run Playwright E2E
              working-directory: fe
              env:
                  E2E_BASE_URL: http://localhost:5173
                  E2E_API_BASE_URL: http://localhost:8080
                  E2E_DATABASE_URL: postgres://my_fam_tree:my_fam_tree@localhost:3458/my_fam_tree
                  MAILPIT_URL: http://localhost:8025
                  WORKER_TEST_URL: http://localhost:9091
              run: pnpm test:e2e
            - if: always()
              uses: actions/upload-artifact@v7
              with:
                  name: playwright-report
                  path: fe/playwright-report/
                  retention-days: 7
            - if: always()
              name: Upload api + worker + vite logs
              uses: actions/upload-artifact@v7
              with:
                  name: e2e-logs
                  path: |
                      /tmp/api.log
                      /tmp/worker.log
                      /tmp/vite.log
                  retention-days: 7

    # ============================================================================
    # Docker build verification - confirm the three published Dockerfiles
    # (api, worker, fe) still build. The migrator + seeder binaries are bundled
    # into the api image (so building api exercises them) and their standalone
    # dev-only Dockerfiles are covered by the compose dev stack, not here. We
    # `load: true` (don't push) so this is a pure build smoke test.
    # ============================================================================
    docker-build:
        needs: changes
        if: ${{ needs.changes.outputs.backend == 'true' || needs.changes.outputs.frontend == 'true' }}
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v4
            - uses: docker/setup-buildx-action@v4
            - name: Build api image
              uses: docker/build-push-action@v7
              with:
                  context: .
                  file: .docker/api.Dockerfile
                  push: false
                  load: true
                  tags: my-fam-tree/api:ci
                  cache-from: type=gha,scope=api
                  cache-to: type=gha,mode=max,scope=api
            - name: Build worker image
              uses: docker/build-push-action@v7
              with:
                  context: .
                  file: .docker/worker.Dockerfile
                  push: false
                  load: true
                  tags: my-fam-tree/worker:ci
                  cache-from: type=gha,scope=worker
                  cache-to: type=gha,mode=max,scope=worker
            - name: Build fe image
              uses: docker/build-push-action@v7
              with:
                  context: .
                  file: .docker/fe/Dockerfile
                  push: false
                  load: true
                  tags: my-fam-tree/fe:ci
                  cache-from: type=gha,scope=fe
                  cache-to: type=gha,mode=max,scope=fe

    # ============================================================================
    # CI Gate - one aggregating check for branch protection. Always runs;
    # fails if any required upstream job failed. Skipped jobs (path filter)
    # don't fail the gate.
    # ============================================================================
    ci-gate:
        needs:
            - changes
            - branch-name
            - commit-lint
            - backend-clippy
            - backend-format
            - backend-audit
            - backend-tests
            - backend-build
            - backend-coverage
            - backend-ts-bindings
            - frontend-lint
            - frontend-typecheck
            - frontend-tests
            - frontend-build
            - frontend-coverage
            - frontend-e2e
            - docker-build
        if: ${{ always() }}
        runs-on: ubuntu-latest
        steps:
            # Strict: every upstream job that was *required to run* must report
            # `success`. `skipped` is treated as failure when the change filter
            # says the group is in scope. If we're triggered by a docs-only PR
            # that doesn't match any filter, all jobs skip and the gate also
            # passes — but that scenario is only reachable when no infra,
            # backend, or frontend paths matched (see the path-filter above —
            # infra changes flip BOTH groups).
            - name: Enforce all upstream jobs
              env:
                  BRANCH_NAME_RESULT: ${{ needs.branch-name.result }}
                  COMMIT_LINT_RESULT: ${{ needs.commit-lint.result }}
                  BACKEND_NEEDED: ${{ needs.changes.outputs.backend }}
                  FRONTEND_NEEDED: ${{ needs.changes.outputs.frontend }}
                  BACKEND_CLIPPY: ${{ needs.backend-clippy.result }}
                  BACKEND_FORMAT: ${{ needs.backend-format.result }}
                  BACKEND_AUDIT: ${{ needs.backend-audit.result }}
                  BACKEND_TESTS: ${{ needs.backend-tests.result }}
                  BACKEND_BUILD: ${{ needs.backend-build.result }}
                  BACKEND_COVERAGE: ${{ needs.backend-coverage.result }}
                  BACKEND_TS_BINDINGS: ${{ needs.backend-ts-bindings.result }}
                  FRONTEND_LINT: ${{ needs.frontend-lint.result }}
                  FRONTEND_TYPECHECK: ${{ needs.frontend-typecheck.result }}
                  FRONTEND_TESTS: ${{ needs.frontend-tests.result }}
                  FRONTEND_BUILD: ${{ needs.frontend-build.result }}
                  FRONTEND_COVERAGE: ${{ needs.frontend-coverage.result }}
                  FRONTEND_E2E: ${{ needs.frontend-e2e.result }}
                  DOCKER_BUILD: ${{ needs.docker-build.result }}
                  EVENT_NAME: ${{ github.event_name }}
              run: |
                  set -e
                  fail=0
                  require_success() {
                      local name="$1"; local result="$2"
                      if [[ "$result" != "success" ]]; then
                          echo "::error::Required job '$name' did not succeed (result=$result)"
                          fail=1
                      else
                          echo "✓ $name"
                      fi
                  }
                  # PR-only gates
                  if [[ "$EVENT_NAME" == "pull_request" ]]; then
                      require_success branch-name "$BRANCH_NAME_RESULT"
                      require_success commit-lint "$COMMIT_LINT_RESULT"
                  fi
                  # Backend group (also covers docker-build + e2e since infra
                  # flips both groups together)
                  if [[ "$BACKEND_NEEDED" == "true" ]]; then
                      require_success backend-clippy       "$BACKEND_CLIPPY"
                      require_success backend-format       "$BACKEND_FORMAT"
                      require_success backend-audit        "$BACKEND_AUDIT"
                      require_success backend-tests        "$BACKEND_TESTS"
                      require_success backend-build        "$BACKEND_BUILD"
                      require_success backend-coverage     "$BACKEND_COVERAGE"
                      require_success backend-ts-bindings  "$BACKEND_TS_BINDINGS"
                  fi
                  # Frontend group
                  if [[ "$FRONTEND_NEEDED" == "true" ]]; then
                      require_success frontend-lint        "$FRONTEND_LINT"
                      require_success frontend-typecheck   "$FRONTEND_TYPECHECK"
                      require_success frontend-tests       "$FRONTEND_TESTS"
                      require_success frontend-build       "$FRONTEND_BUILD"
                      require_success frontend-coverage    "$FRONTEND_COVERAGE"
                  fi
                  # E2E + docker-build run when either group is in scope
                  if [[ "$BACKEND_NEEDED" == "true" || "$FRONTEND_NEEDED" == "true" ]]; then
                      require_success frontend-e2e         "$FRONTEND_E2E"
                      require_success docker-build         "$DOCKER_BUILD"
                  else
                      echo "No backend or frontend changes — nothing required to check."
                  fi
                  if [[ $fail -eq 1 ]]; then
                      echo "::error::CI gate failed"
                      exit 1
                  fi
                  echo "CI gate passed"
